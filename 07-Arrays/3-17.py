tuple = (50,20,40,50,30,50)

#for element in set(tuple):
    #count = tuple.count(element)

value = 50
count = tuple.count(value)
    
print('Tuple:', " ".join(map(str, tuple)))
print('Value:', value)
print('Occurrences:', count)