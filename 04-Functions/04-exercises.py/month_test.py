import month 
n = int(input('enter the month number'))#create an int variable 

print(f"The name of the month {n} is {month.month(n)}")# we call the function into {} 
# in form {module_name.function_name(variable_name)}